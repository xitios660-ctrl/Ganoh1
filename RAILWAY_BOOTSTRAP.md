# Railway bootstrap

This marker exists only to trigger the first Railway deployment for the Ganoh backend. No application behavior is changed.

- Backend stays in maintenance mode during bootstrap.
- Scheduler stays disabled.
- WhatsApp sending stays disabled.
- Financial changes remain isolated on `ganoh/staged-audit`.
